---
title: "[260218] TIL"
excerpt: ""

categories:
  - TIL
tags:
  - [TIL]

permalink: /blog/til/260218

toc: true
toc_sticky: true

date: 2026-02-18
last_modified_at: 2026-02-18
---

# Today I Learned (2026-02-18)

## useActionState

- `useActionState`는 폼 액션 결과를 기반으로 state를 업데이트하는 훅이다.
- 이전 버전의 `useFormState` 명칭에서 변경되었다.

```js
const [state, formAction, isPending] = useActionState(fn, initialState, permalink?)
```

## useFormStatus

- `useFormStatus`는 폼 제출 진행 상태(`pending`)를 알려주는 훅이다.

```jsx
"use client"
import { useFormStatus } from "react-dom"

function SubmitButton() {
  const { pending } = useFormStatus()

  return (
    <button disabled={pending}>
      {pending ? "Saving..." : "Save"}
    </button>
  )
}
```
